# Pain-points core

`rank-pain-points.ts` owns the exact versioned pain taxonomy and deterministic
component arithmetic for model `1.0.0`. It emits only findings backed by valid
Business Twin evidence and sorts by priority, impact, then stable pain ID.
