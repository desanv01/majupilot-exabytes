import { z } from "zod";import { correlationId,errorResponse,readJson,repository,resolveOwner,response } from "@/infrastructure/persistence/api";import { PersistenceService } from "@/core/persistence/persistence-service";import { dataRequestSchema,persistenceUuidSchema } from "@/domain/persistence";export const runtime="nodejs";const schema=z.object({organizationId:persistenceUuidSchema.optional(),request:dataRequestSchema}).strict();
export async function POST(request:Request){const id=correlationId(request);try{const b=schema.parse(await readJson(request,16*1024));const owner=await resolveOwner(request,b.organizationId);const receipt=await new PersistenceService(repository()).requestDeletion(owner,b.request);return response({data:{requestId:b.request.id,status:"pending",receipt}},202,id);}catch(e){return errorResponse(e,id);}}
