import { spawn } from "node:child_process";
import { mkdir, writeFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import path from "node:path";

const chromePath = "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe";
const port = 9444;
const baseUrl = "http://localhost:3000";
const artifacts = path.resolve("artifacts");
const profile = path.join(tmpdir(), `sme-growth-twin-stage04-${process.pid}`);
await mkdir(artifacts, { recursive: true });
const chrome = spawn(chromePath, ["--headless=new", "--disable-gpu", "--no-first-run", "--no-default-browser-check", `--remote-debugging-port=${port}`, `--user-data-dir=${profile}`, "about:blank"], { stdio: "ignore" });
const wait = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
let endpoint;
for (let attempt = 0; attempt < 40; attempt += 1) { try { const tabs = await fetch(`http://127.0.0.1:${port}/json`).then((response) => response.json()); endpoint = tabs.find((tab) => tab.type === "page" && tab.url === "about:blank")?.webSocketDebuggerUrl ?? tabs.find((tab) => tab.type === "page")?.webSocketDebuggerUrl; if (endpoint) break; } catch {} await wait(250); }
if (!endpoint) throw new Error("Chrome DevTools endpoint did not start");
const ws = new WebSocket(endpoint); await new Promise((resolve, reject) => { ws.addEventListener("open", resolve, { once: true }); ws.addEventListener("error", reject, { once: true }); });
let nextId = 0; const pending = new Map(); const consoleErrors = [];
ws.addEventListener("message", (event) => { const message = JSON.parse(event.data); if (message.id && pending.has(message.id)) { const pendingCall = pending.get(message.id); pending.delete(message.id); if (message.error) pendingCall.reject(new Error(message.error.message)); else pendingCall.resolve(message.result); } if (message.method === "Runtime.exceptionThrown") consoleErrors.push(message.params.exceptionDetails.text); if (message.method === "Runtime.consoleAPICalled" && message.params.type === "error") consoleErrors.push(message.params.args.map((arg) => arg.value ?? arg.description).join(" ")); });
const cdp = (method, params = {}) => new Promise((resolve, reject) => { const id = ++nextId; pending.set(id, { resolve, reject }); ws.send(JSON.stringify({ id, method, params })); });
const evaluate = async (expression) => (await cdp("Runtime.evaluate", { expression, awaitPromise: true, returnByValue: true })).result.value;
const poll = async (expression, expected, timeout = 15000) => { const started = Date.now(); let actual; while (Date.now() - started < timeout) { actual = await evaluate(expression); if (actual === expected) return; await wait(150); } throw new Error(`Timed out: ${expression}; actual=${JSON.stringify(actual)}`); };
const navigate = async (url) => { await cdp("Page.navigate", { url }); await poll("document.readyState", "complete"); };
const screenshot = async (name, full = true) => { let params = { format: "png", fromSurface: true }; if (full) { const metrics = await cdp("Page.getLayoutMetrics"); params = { format: "png", captureBeyondViewport: true, clip: { x: 0, y: 0, width: Math.ceil(metrics.cssContentSize.width), height: Math.ceil(metrics.cssContentSize.height), scale: 1 } }; } const capture = await cdp("Page.captureScreenshot", params); await writeFile(path.join(artifacts, name), Buffer.from(capture.data, "base64")); };

await cdp("Page.enable"); await cdp("Runtime.enable"); await cdp("Log.enable"); await cdp("Emulation.setDeviceMetricsOverride", { width: 1440, height: 1000, deviceScaleFactor: 1, mobile: false }); await navigate(baseUrl);
const draft = { schemaVersion: "1.0.0", sessionId: "assessment_browser0401", status: "ready_for_review", currentStep: 6, twinRevision: 1, answers: { q1: { businessName: "Kopi Kita Café Group", industry: "food_beverage", businessModel: "b2c", employeeBand: "25_49", description: "Three Malaysian café outlets serving walk-in and catering customers." }, q2: { websiteOrStore: "active", businessEmail: "not_used", cloudProductivity: "informal", crm: "not_used", digitalMarketingAnalytics: "active", backup: "not_used", cybersecurityControls: "informal", aiTools: "not_used" }, q3: { biggestChallenge: "customer_management", manualWorkflow: "WhatsApp orders and catering enquiries", manualHoursPerWeek: null, affectedEmployees: 8, urgency: 4 }, q4: { primaryObjective: "increase_revenue", budgetBand: "5k_15k", implementationPace: "1_3_months", highestConcern: "cost" }, q5: { leadershipSponsorship: 4, usableData: 2, employeeDigitalSkills: 3, processConsistency: 2, changeWillingness: 4 } }, selectedFollowUpIds: ["fu_manual_hours", "fu_customer_records", "fu_backup_frequency"], followUpAnswers: { fu_manual_hours: "11_20", fu_customer_records: "messaging_apps", fu_backup_frequency: "none" }, updatedAt: "2026-09-17T09:00:00+08:00" };
await evaluate(`localStorage.setItem("sme-growth-twin:assessment-draft:1.0.0", ${JSON.stringify(JSON.stringify(draft))})`);
await navigate(`${baseUrl}/assessment/analysis`); await poll("location.pathname", "/results", 12000); await evaluate("document.querySelector('a[href=\"/recommendations\"]')?.click()"); await poll("location.pathname", "/recommendations"); await poll("document.body.innerText.includes('Compare transformation scenarios')", true); await evaluate("document.querySelector('a[href=\"/scenarios\"]')?.click()"); await poll("location.pathname", "/scenarios"); await poll("document.body.innerText.includes('Accelerated AI')", true);
const defaultValues = await evaluate("JSON.stringify(JSON.parse(localStorage.getItem('sme-growth-twin:scenarios:1.0.0')).scenarios.map(s=>({id:s.templateId,cost:s.costs.firstYear,net:s.value.net,payback:s.value.payback,conditional:s.costs.conditionalExpansionCost})))");
const desktopOverflow = await evaluate("document.documentElement.scrollWidth <= window.innerWidth"); const desktopTargets = await evaluate("Math.min(...Array.from(document.querySelectorAll('a,button,summary,input')).map(e=>e.getBoundingClientRect().height).filter(Boolean))");
await screenshot("stage-04-scenario-comparison-desktop.png");
const balancedButtons = await evaluate("Array.from(document.querySelectorAll('.balanced_growth button')).map(b=>b.textContent.trim())");
await evaluate("Array.from(document.querySelectorAll('.balanced_growth button')).find(b=>b.textContent.includes('Select as preferred'))?.click()"); await wait(200);
const preferredPersisted = await evaluate("Boolean(JSON.parse(localStorage.getItem('sme-growth-twin:scenarios:1.0.0')).selectedScenarioId)");
const oldBase = await evaluate("JSON.parse(localStorage.getItem('sme-growth-twin:scenarios:1.0.0')).scenarios.find(s=>s.templateId==='balanced_growth').value.operational.range.base");
await evaluate(`(()=>{const input=Array.from(document.querySelectorAll('.operational input')).find(i=>i.value==='25');const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set;setter.call(input,'30');input.dispatchEvent(new Event('input',{bubbles:true}));})()`); await wait(250);
const newBase = await evaluate("JSON.parse(localStorage.getItem('sme-growth-twin:scenarios:1.0.0')).scenarios.find(s=>s.templateId==='balanced_growth').value.operational.range.base");
await screenshot("stage-04-scenario-detail-assumptions-desktop.png");
const beforeInvalid = newBase; await evaluate(`(()=>{const input=Array.from(document.querySelectorAll('.operational input')).find(i=>i.value==='30');const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set;setter.call(input,'');input.dispatchEvent(new Event('input',{bubbles:true}));})()`); await wait(150);
const afterInvalid = await evaluate("JSON.parse(localStorage.getItem('sme-growth-twin:scenarios:1.0.0')).scenarios.find(s=>s.templateId==='balanced_growth').value.operational.range.base"); const invalidExplained = await evaluate("Boolean(document.querySelector('.operational input[aria-invalid=\"true\"]'))");
await cdp("Page.reload", { ignoreCache: true }); await poll("document.body.innerText.includes('Accelerated AI')", true); const preferenceRestored = await evaluate("document.body.innerText.includes('Preferred')");
await cdp("Emulation.setDeviceMetricsOverride", { width: 360, height: 800, deviceScaleFactor: 1, mobile: true }); await cdp("Page.reload", { ignoreCache: true }); await poll("document.body.innerText.includes('Accelerated AI')", true);
const mobileOverflow = await evaluate("document.documentElement.scrollWidth <= window.innerWidth"); const mobileTargets = await evaluate("Math.min(...Array.from(document.querySelectorAll('a,button,summary,input')).map(e=>e.getBoundingClientRect().height).filter(Boolean))"); const mobileColumns = await evaluate("getComputedStyle(document.querySelector('.scenario-grid')).gridTemplateColumns"); const overlay = await evaluate("Boolean(document.querySelector('[data-nextjs-dialog], .vite-error-overlay, #webpack-dev-server-client-overlay'))");
await screenshot("stage-04-scenario-mobile-360-viewport.png", false); await screenshot("stage-04-scenario-mobile-360-full.png"); await cdp("Input.dispatchKeyEvent", { type: "keyDown", key: "Tab", code: "Tab", windowsVirtualKeyCode: 9 }); await cdp("Input.dispatchKeyEvent", { type: "keyUp", key: "Tab", code: "Tab", windowsVirtualKeyCode: 9 }); const focusedTag = await evaluate("document.activeElement?.tagName");
console.log(JSON.stringify({ route: await evaluate("location.pathname"), defaultValues: JSON.parse(defaultValues), desktopOverflow, desktopTargets, balancedButtons, preferredPersisted, oldBase, newBase, invalidPreservesLastValid: beforeInvalid === afterInvalid, invalidExplained, preferenceRestored, mobileOverflow, mobileTargets, mobileColumns, focusedTag, overlay, consoleErrors }, null, 2));
await cdp("Browser.close"); ws.close(); chrome.kill();
